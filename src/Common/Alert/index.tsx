import * as S from "./styles";
import { AlertProps } from "./types";

const Alert: React.FC<AlertProps> = ({ severity, children }) => {
  return <S.AlertWrapper></S.AlertWrapper>;
};

export default Alert;
